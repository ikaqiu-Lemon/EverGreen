// [S1] internal/git：init/add/commit/status/diff 与提交信息规范化。
//
// 允许依赖：model。
// 首次落地：S1（施工索引 §13）。
//
// 安全底线 B4（结构性保证）：Git 提交失败时**不做破坏性回滚**——本包不导出、
// 也不实现任何回滚 / 还原 / 撤销入口，不删除文件、不还原内容，只保留磁盘现状，
// 把「未提交变更清单 + 失败原因」交给上层退 4。ADR-05 的回滚方案已废弃，
// 任何阶段都不得重新实现（本文件与本包其它文件均不含任何撤销类 Git 子命令）。
//
// 提交范围唯一口径：Add 走 `git add -A`——本次写入的改动与**工作区已有改动一并
// 进本次 commit**，由上层在报告中如实说明。S1 不做对账、不做工作区隔离式的选择性
// 提交（那属 S3 / S5，提前实现即越界）；Status / Diff 在 Add 前采样，产出「本次改动」
// 与「既有改动」两份路径清单供上层如实上报。
package git
